# @repogotchi/core

Shared TS engine: scoring (health/happiness from raw signals), mood mapper, evolution stage logic, hysteresis state machine. Surface-agnostic — used by the CLI, Worker, GitHub Action, and macOS companion (via the Swift port).

Depends on: `@repogotchi/contract`.

## Status

Planned. Lands after the contract package.
